import QtQuick 2.0

Item {

    readonly property string level: qsTr("Уровень") + mytrans.emptyString

    readonly property string riht_Left: qsTr("Рихтовка левая") + mytrans.emptyString

    readonly property string riht_Right: qsTr("Рихтовка правая") + mytrans.emptyString

    readonly property string sample: qsTr("Шаблон") + mytrans.emptyString

    readonly property string down_Left: qsTr("Просадка левая") + mytrans.emptyString

    readonly property string down_Right: qsTr("Просадка правая") + mytrans.emptyString

    readonly property string speed: qsTr("Скорость") + mytrans.emptyString

    readonly property string siteID: qsTr("Код дороги") + mytrans.emptyString

    readonly property string upNom: qsTr("Код направления") + mytrans.emptyString

    readonly property string putNom: qsTr("Номер пути") + mytrans.emptyString

}
